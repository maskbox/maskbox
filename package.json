{
	"name": "root",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"preinstall": "npx only-allow pnpm",
		"format": "prettier --write .",
		"dev:docker": "docker compose up -d"
	},
	"devDependencies": {
		"prettier": "^2.7.1",
		"typescript": "^4.8.2"
	}
}
