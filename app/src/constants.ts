export const MAX_EMAILS_PER_ACCOUNT = 4;
export const MAX_MASKS_PER_ACCOUNT = 24;

// TTL for email verification token in milliseconds (1 day):
export const EMAIL_VERIFICATION_TOKEN_TTL = 86400 * 1000;

// TTL for signature in milliseconds (1 minute):
export const SIGNATURE_TTL = 60 * 1000;
