export const RELAY_DOMAIN = 'relay.maskbox.app';

export const MAX_EMAILS_PER_ACCOUNT = 4;
export const MAX_MASKS_PER_ACCOUNT = 24;

// Max age for sign in code in seconds (10 minutes):
export const SIGN_IN_CODE_MAX_AGE = 600;

// Max age for email verification token in milliseconds (1 day):
export const EMAIL_VERIFICATION_MAX_AGE = 86400 * 1000;

// Max age for signature in milliseconds (1 minute):
export const SIGNATURE_MAX_AGE = 60 * 1000;
